fun main() {
    println("Digite um falor para N !!")

    //val n = 1..(readLine() ?: return).toInt()
    val n =1
   // n.forEach {
        //println("$it ${(it.toDouble().pow(2)).toInt()} ${(it.toDouble().pow(3)).toInt()}")
        //println(            )
    println("Valor de N é $n")

}